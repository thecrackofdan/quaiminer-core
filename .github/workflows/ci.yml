name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: miner-dashboard/package-lock.json
    
    - name: Install dependencies
      working-directory: ./miner-dashboard
      run: |
        if [ -f package-lock.json ]; then
          npm ci || npm install
        else
          npm install
        fi
    
    - name: Run syntax check
      working-directory: ./miner-dashboard
      run: npm test
    
    - name: Run security audit
      working-directory: ./miner-dashboard
      run: npm audit --audit-level=moderate || true
    
    - name: Check code quality
      working-directory: ./miner-dashboard
      run: |
        node -c server.js
        node -c database.js
        node -c auth.js
        echo "✅ All syntax checks passed"

  build-docker:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Docker image
      working-directory: ./miner-dashboard
      run: |
        docker build -t quaiminer-dashboard:latest .
    
    - name: Test Docker image
      working-directory: ./miner-dashboard
      run: |
        docker run -d --name test-dashboard -p 3000:3000 quaiminer-dashboard:latest
        sleep 5
        curl -f http://localhost:3000/api/health || exit 1
        docker stop test-dashboard
        docker rm test-dashboard

  release:
    runs-on: ubuntu-latest
    needs: [test, build-docker]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - uses: actions/checkout@v3
    
    # Note: Automated releases are handled by release.yml workflow
    # This job is disabled to avoid duplicate releases
    - name: Skip automated release
      run: echo "Releases are handled by release.yml workflow"

